import 'package:flutter/material.dart';

class ShopTap extends StatelessWidget {
  const ShopTap({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      color: Colors.red,
    );
  }
}
