import 'package:flutter/material.dart';

class LibraryTap extends StatelessWidget {
  const LibraryTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}
